let num = [5, 2, 6, 9];

//console.log(`O vetor tem o tamanho ${num.length}`);
//console.log(`O vetor em ordem crescente fica ${num.sort()}`);

for(let pos = 0; pos < num.length; pos++){
    console.log("A posição " + pos + " tem o valor " + num[pos]);
}

console.log(num.indexOf(2));